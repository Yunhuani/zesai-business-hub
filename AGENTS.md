# AGENTS.md

本文件适用于整个仓库。若某个子目录中存在更深层的 `AGENTS.md`，则该文件可为其目录范围补充或覆盖规则。

## 工作原则

1. 编码前明确目标、假设和成功标准。存在多种合理解释时，先说明差异；会显著改变结果或风险的歧义应先询问用户。
2. 优先选择能够完整解决问题的最简单方案。不要增加未要求的功能、抽象、配置项或兼容层。
3. 保持最小、可追溯的修改范围。每一处改动都应直接服务当前请求，不顺手重构、格式化或删除无关代码。
4. 修改前确定验证方式；修改后运行与风险相称的检查，并如实报告失败、跳过项和环境限制。

## 项目边界

- `client/`：React 前端页面、组件、hooks、样式和浏览器侧逻辑。
- `server/`：Express/tRPC 接口、认证、业务逻辑、外部集成和服务端测试。
- `shared/`：前后端共同使用的类型、常量和纯规则；不要在此引入浏览器或服务端专属依赖。
- `drizzle/`：数据库 schema、关系和迁移文件。
- `scripts/`：一次性初始化、迁移和运维工具；新增脚本时说明目标环境和安全边界。
- `docs/`：部署、设计和运维文档。

遵循现有模块边界。除非请求本身需要，不要移动文件或重组目录。

## 实现约束

- 使用现有 TypeScript 与 ES module 风格，匹配相邻代码的命名、导入和错误处理方式。
- 不修改与任务无关的格式、注释或文案。仓库已有未提交改动时，默认其属于用户并予以保留。
- 只清理本次改动造成的无用 import、变量、函数或文件；发现既有问题可在交付说明中指出，不要擅自扩大范围。
- 优先复用现有依赖和工具。新增或升级依赖必须是完成请求所必需，并说明理由和影响。
- 不手工编辑 `dist/` 或其他生成产物；通过项目脚本重新生成。
- 保持前后端契约一致。修改 tRPC router、共享类型或 API 响应时，检查所有调用方和相关测试。

## 数据与安全

- 不读取、输出、提交或复制 `.env` 中的真实值。环境变量名称和示例以 `.env.example` 为准。
- 不把 API key、JWT secret、支付私钥、OAuth secret、访问令牌或生产连接字符串写入源码、测试、日志或文档。
- 未经明确授权，不执行生产迁移、批量数据修改、删除、回滚或不可恢复操作。
- 执行数据脚本前确认目标数据库、影响范围、幂等性和恢复方式；优先使用隔离的开发或测试数据。
- 支付、认证、积分扣减、权限和文件下载路径属于高风险区域。修改时必须覆盖失败、重试、重复请求和越权场景。
- 日志与错误响应不得泄露密钥、令牌、完整支付数据或不必要的个人信息。

## 数据库规则

- 默认 `drizzle.config.ts` 使用 `drizzle/schema.ts`，dialect 为 MySQL。
- PostgreSQL 使用 `drizzle.config.postgres.ts`、`drizzle/schema-postgres.ts` 和 `drizzle/migrations/`；不要假设两套 schema 自动同步。
- schema 变更必须包含相应迁移，并检查现有数据、索引、默认值、空值和回滚风险。
- 不修改或重写已经应用的历史迁移；使用新的迁移表达后续变化。
- `npm run db:push` 会生成并执行默认 Drizzle 迁移。运行前必须确认 `DATABASE_URL` 指向预期的非生产环境。

## 测试与验证

先运行最接近改动的测试，再根据影响范围扩大验证：

```bash
npm run check
npm test
npm run build
```

在 Windows PowerShell 因执行策略无法运行 `npm.ps1` 时，使用 `npm.cmd` 执行相同脚本。

- 小型纯文档变更：检查链接、命令、路径、Markdown 和最终 diff；必要时运行完整质量检查以记录基线状态。
- 前端改动：运行相关组件或页面测试、类型检查和构建；涉及交互或布局时在浏览器中验证关键流程和响应式表现。
- 后端改动：运行相关 Vitest 文件、类型检查和构建；接口改动同时检查权限、输入验证和调用方。
- 数据库、认证、支付或积分改动：增加针对性回归测试，再运行完整测试套件和构建。
- 如果完整检查因缺少外部环境变量或既有问题失败，记录准确命令和核心错误。不要为使检查变绿而修改无关代码，也不要伪造密钥。

`npm run format` 会格式化整个仓库。除非用户明确要求或变更范围已经受控，不要在存在无关修改的工作区直接运行；可对本次修改的文件使用更窄的格式化命令。

## Git 与变更管理

- 不覆盖、回退、暂存或提交用户已有的修改，除非用户明确要求。
- 不使用 `git reset --hard`、`git checkout --` 等破坏性命令处理未提交内容。
- 提交前检查 `git diff` 和 `git status --short`，确认改动范围与请求一致。
- 只有在用户明确要求时才创建分支、提交、推送或 Pull Request。

## 完成标准

任务只有在以下条件满足时才算完成：

- 用户要求的行为或文档已经实现，没有额外功能。
- 相关测试、类型检查和构建已按风险运行，结果已核实并如实说明。
- 最终 diff 只包含任务需要的改动，并保留用户原有修改。
- 接口、环境变量、数据库或运维流程发生变化时，相应测试和文档已同步更新。
- 没有遗留本次修改产生的无用代码、调试输出、占位符或敏感信息。
